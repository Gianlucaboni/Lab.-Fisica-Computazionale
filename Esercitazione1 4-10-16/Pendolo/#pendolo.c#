#include<stdio.h>
#include<math.h>
#define g 9.81
#define passo 0.001
struct set parametri{
  double alpha;
  double v;
  double L;
};
double lettura(char *);
double energia(struct set);
double accelerazione(double, struct set);

int